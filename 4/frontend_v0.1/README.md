# medical-beauty

> A Vue.js project

## Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build:test
npm run build:prod

# api (src目录下)
1. 所有接口统一在 api.js 中定义

# 路由配置
1. 先在 routepath.js 中定义
2. 在 router.js 中配置
